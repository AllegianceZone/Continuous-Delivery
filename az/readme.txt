These files go onto the allegiancezone Azure VM in C:\deploy.

service.pl is instsrv'ed as 'deploy' service using srvany

azure-sync.bat requires AZCopy be installed and put into the system PATH environment variable